<% content_for(:page_title) { ("#{@project.title}") } %>
<%= image_tag @project.inspiration_image %>
<div class="wrapper">
<h1><%= @project.title %></h1>
<h3><%= @project.level.capitalize %> Academy</h3>

<h2>Project Overview</h2>
<% %w(synopsis skills).each do |section| %>
<div id="<%= section %>">
  <h3><%= section.capitalize %></h3>
  <p><%= @project.send(section.to_sym)%></p>
</div>
<% end %>

<h2> Set-Up at a Glance</h2>
<h3>Materials</h3>
<p>
<% @project.supply_items.each do |supply| %>
  <% capitalized_name = supply.name.capitalize %>
  <% if supply.has_link? %>
    <%= link_to capitalized_name, supply.url %>,
  <% else %>
    <%= capitalized_name %>,
  <% end %>
<% end %>
</p>

<% %w(books_media photocopies).each do |section| %>
<div id="<%= section %>">
  <h3><%= section.capitalize %></h3>
  <p><%= @project.send(section.to_sym)%></p>
</div>
<% end %>
</div>

<h2>Standards</h2>
<% %w(national, common_core, art_elements, art_principles).each do |section| %>
  <h3><%= section.capitalize %></h3>
  <p><%= @project.standard.send(section.to_sym) %></p>
<% end %>
 
