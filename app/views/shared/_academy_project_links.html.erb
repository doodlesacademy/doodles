<% if academy == :lower %>
  Lower Academy /
  <%= link_to "Upper Academy", project_path("upper", @project.slug) %>
<% else %>
  <%= link_to "Lower Academy", project_path("lower", @project.slug) %>
  / Upper Academy
<% end %>
