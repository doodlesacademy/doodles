<% path = @supply_item.persisted? ? admin_supply_item_path(@supply_item) : admin_supply_items_path %> 
<%= form_for @supply_item, url: path do |f| %>
  <% if @supply_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@supply_item.errors.count, "error") %> prohibited this from being saved:</h2>

      <ul>
      <% @supply_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :asin, "Amazon Product ID" %>
    <%= f.text_field :asin %>
  </div>
  
  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
