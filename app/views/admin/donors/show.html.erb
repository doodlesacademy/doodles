<%= link_to 'Edit', edit_admin_donor_path(@donor) %> |
<%= link_to 'Back', admin_donors_path %>

<%= render "shared/alert", alert: notice %>

<p>
  <strong>First name:</strong>
  <%= @donor.first_name %>
</p>

<p>
  <strong>Last name:</strong>
  <%= @donor.last_name %>
</p>

<%= render "shared/destroy_button", entity: @donor %>
